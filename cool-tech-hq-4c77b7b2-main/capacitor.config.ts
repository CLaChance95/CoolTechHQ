import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cooltechhq.app',
  appName: 'CoolTechHQ',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
